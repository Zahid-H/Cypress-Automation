/// <reference types="cypress"/>

describe('Automation of chaldal Multiple product order', () => {
    
    beforeEach(() => {
        //visiting the the chaldal URL 
        cy.visit('https://chaldal.com/')
    });
    it('', () => {
        
    });
});